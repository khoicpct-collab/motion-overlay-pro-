{
  "name": "motion-overlay-pro",
  "version": "1.0.0",
  "description": "Motion Overlay GIF Simulator",
  "scripts": {
    "dev": "live-server . --port=3000",
    "build": "echo 'No build step' && exit 0",
    "start": "live-server . --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14"
  }
}
